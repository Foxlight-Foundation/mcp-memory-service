# Published image tag for memory-manager (or keep build-only)
MEMORY_MANAGER_IMAGE=ttupper75035/mcp-memory-service:latest

# Host ports
MEMORY_MANAGER_PORT=8000
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334

# Ollama endpoint (local mini or remote mini)
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Memory model preferences (used by manager as it evolves)
MEMORY_LLM_MODEL=qwen3:8b
MEMORY_EMBED_MODEL=nomic-embed-text
