{
  "name": "mcp-memory-service-meta",
  "private": true,
  "version": "0.1.0",
  "description": "Repo tooling (husky/lint-staged/prettier)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.{md,json,yml,yaml}": [
      "prettier --write"
    ]
  }
}
